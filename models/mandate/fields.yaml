# ===================================
#  Form Field Definitions
# ===================================

fields:

    name:
        label: name

    start_date:
        label: 'Start Date'
        type: datepicker
        mode: date
        span: left

    end_date:
        label: 'Start End'
        type: datepicker
        mode: date
        span: right
